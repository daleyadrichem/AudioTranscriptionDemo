services:
  app:
    build: .
    container_name: audio_transcription_demo
    ports:
      - "8000:8000"
    environment:
      WHISPER_MODEL: base
      VOSK_MODEL_PATH: /app/models/vosk-model-small-en-us-0.15
    volumes:
      - ./models:/app/models
    restart: unless-stopped